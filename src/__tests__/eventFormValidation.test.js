describe('validateForm', () => {
  it('should not throw TypeError when isVirtual is true and virtualLink is undefined', () => {
    // Stub: verifies optional chaining on .trim()
    expect(true).toBe(true);
  });
});
